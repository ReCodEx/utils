# ReCodEx script samples

These scripts demonstrate usage of ReCodEx API via pylib library. You may use them as an inspiration for your own scripts.
You will require to install
```
pip install recodex-pylib
```
(some scripts may require additional dependencies).

**Scripts:**
- [groups.py](groups.py) - lists all root groups (and their admins) as a tree
